# Optional: Gemini (legacy AI Studio)
GEMINI_API_KEY=

# Public app URL (used in docs / OAuth callbacks)
APP_URL=http://localhost:3001

# --- EmailJS (optional — also configurable in Settings UI) ---
# Create free keys at https://www.emailjs.com/
# Template variables: to_email, to_name, from_name, subject, message
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
